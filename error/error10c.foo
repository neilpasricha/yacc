main()
{
    int x;
    bool b;
    x = x + (1/2+x%3)*2;
    b = x > 10;
    b = b or true and false;

    print x;

    if( not 3.141592 ) {
        int y;
        y = y + x;
    } else {
    }
}
