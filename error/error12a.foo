main()
{
    int x;
    bool b;
    x = x + (1/2+x%3)*2;
    b = x > 10;
    b = b or true and false;

    print x;

    if( b ) {
        int y;
        y = y + x;
    } else {
    }

    while ( b != true )
    {
        int y;
        y = y + 2;
        x = x * y;
    }

    return 10;
}
