main()
{
    int x;
    bool b;
    x = x + (1/2+x%3)*2;
    b = x > 10;
    b = b or true and false;

    print x;

    if( 1 == true ) {
        int y;
        y = y + x;
    } else {
    }
}
